import { DialogContent } from "@mui/material";
import { styled } from "@mui/material/styles";

export const BasicDialogContent = styled(DialogContent)(({ theme }) => ({
  padding: "12px 12px",
  maxHeight: "70vh",
}));

export default BasicDialogContent;
